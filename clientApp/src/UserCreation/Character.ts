export class Character {
    public sprite: string;
    public color: string;
    public username: string;

    constructor(sprite: string, color: string, username: string) {
        this.sprite = sprite;
        this.color = color;
        this.username = username;
    }
}